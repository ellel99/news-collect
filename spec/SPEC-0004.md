# SPEC-0004 — First Approved Polling Source Pilot

状态：Active — Docs Review（implementation not started）
阶段：Phase 1 — Information Collection & Push
负责人：Project Owner
创建日期：2026-07-29
最后更新：2026-07-29

## 1. 目标

定义第一个合法、低成本或公开 Polling Source Pilot 的实现合同，为后续真实来源接入提供
provider-neutral 模板。本 SPEC 的文档进入审核不代表实现授权。用户已选择 GDELT 作为唯一
试点 candidate；这不表示 endpoint、授权、条款或运行参数已经验证。本文档 Review PASS 且
所有 implementation 前核验完成后，才可另行授权实现。

未来实现必须验证：

- SPEC-0003 collection framework 能接入一个经批准的真实 polling source；
- 既有 `Source`、`SourceAccount`、`CollectionCursor`、`CollectionRun`、`RawItem` 能正确
  承载真实来源运行；
- cursor、watermark、retry、idempotency、有限 backfill 和 access policy 合同可执行；
- downstream 只依赖统一内部 envelope，不依赖 provider SDK 或 raw payload；
- 无 schema 变化，且不实现 downstream normalization、dedup、outbox、AI 或 Event。

## 2. 背景

SPEC-0003 已完成并通过 fake adapter 验证来源无关采集框架。下一步只能选择一个合法来源，
以最小范围证明真实 Polling 接入，同时保持 `Source Adapter / Connector → 统一 envelope →
现有 collection runner` 边界。

PR #6 记录了长期 provider-neutral 架构方向。SPEC-0004 只采用与当前 Phase 1 一致的
Polling adapter 边界，不批准 Streaming、Webhook、通用 Historical Backfill、Internal
Event Bus、Event、AI、Market Validation 或 Research Recommendation。

## 3. Foundation 与阶段边界

- 使用的冻结 Decision：D-003、D-006、D-009、D-010、D-012、D-014、D-015、D-018、
  D-019，以及 Foundation v2.1-FROZEN 的 Content First、合法授权、Failure Visibility。
- 当前阶段允许：一个经用户批准的合法 Polling Source、provider-specific adapter mapping、
  RawItem 留痕、CollectionRun、cursor/watermark、retry、幂等与有限回补。
- 当前阶段禁止：AI/LLM、Event、Evidence、Analysis、Portfolio、Holding、Investment Plan、
  Candidate Rule、Market Validation、Research Recommendation、交易建议和自动交易。
- D-020–D-024 仍是 Proposed Decisions；本 SPEC 不把它们变为 Foundation 生效事实。
- 是否修改 Foundation：否。
- 是否修改 schema：否。
- 是否开始实现：否；首个 provider candidate 已确认为 GDELT，但本文档必须先 Review PASS，
  且第 7 节所有 implementation blocker 必须完成真实核验。

## 4. 前置条件与依赖

- 前置 SPEC：SPEC-0003 已完成并合并，tag `spec-0003-completed`。
- 基础设施：SPEC-0001–0003 的 Python/PostgreSQL/Redis/Celery、九个 Phase 1 业务表、
  CollectionAdapter contract、AdapterRegistry、scheduler、runner、lock 和 retry framework。
- 外部权限：GDELT candidate 已由用户确认；endpoint、认证、terms/robots、许可、保存范围、
  rate limit 和真实访问仍待实现前核验。任何 smoke test、endpoint 访问或真实数据获取都必须
  等到这些核验通过。
- 实现前用户确认事项：
  1. 唯一试点 provider：GDELT（已确认）；具体 Source/SourceAccount 配置待审核；
  2. 合法 endpoint/feed/public page；
  3. authentication、terms/robots、许可、保存范围与请求频率；
  4. access level、license policy、retention class 和 backfill window。

### 4.1 Provider Selection Gate

用户已选择 GDELT 作为 SPEC-0004 唯一 Polling Source Pilot candidate。这一选择只确定
provider，不证明授权、endpoint、可用性、许可、实时等级或内容范围，也不使 GDELT 成为
核心依赖。

RSS / Atom、CNBC、Reuters、公司 IR、SEC/政府机构、NewsAPI.ai / Event Registry、Finnhub
及其他来源均不属于本 SPEC，不得作为 fallback 或第二 provider 接入。

选择规则：

- 具体第一个 provider 已由用户明确确认为 GDELT；
- GDELT implementation 仍不得开始，直到本文档 Review PASS 且第 7 节待核验项完成；
- GDELT 必须可替换，不得成为 framework 或 downstream 的核心依赖；
- 未授权、未知、阻塞或合同不完整的能力必须 fail closed；
- 不得从一个失败接入方式 fallback 到网页抓取、登录绕过或其他未批准方式；
- 只允许一个试点来源，禁止顺带接入第二个 provider。

## 5. 范围

- 为用户选择的 GDELT Polling Source 定义并在后续获准后实现一个 provider-specific
  Adapter/Connector。
- 将 provider 响应映射为 SPEC-0003 的统一 `RawItemEnvelope` / `FetchBatch` 合同。
- 使用既有 Source Registry、scheduler、CollectionRun、RawItem persistence、Redis lock、
  retry 和 cursor checkpoint。
- 使用既有 `Source.access_method` 注册唯一 adapter key；未知 key fail closed。
- 在既有 `SourceAccount.collection_options` 中承载经审核的非秘密来源合同配置，包括
  access/license、cursor、水位、轮询和有限 backfill 规则；不得新增字段。
- 根据许可仅保存允许范围的 payload、摘要、标题、链接、元数据、hash 或受控引用。
- 增加未来实现所需的 fixture、contract、集成和授权后 smoke test。
- 更新来源配置模板、Source Catalog 状态、Changelog 和 Delivery Report，仅限未来实现 PR。

## 6. 非范围

- Streaming Source、WebSocket、Kafka 或商业实时流。
- Webhook Source。
- Historical Backfill 的完整通用框架；只允许被选 provider 明确支持且经批准的有限时间窗
  或 cursor 回补。
- X Source 或任何 X 账号。
- Telegram Bot 或 Telegram 投递。
- Notification 策略、Outbox publisher/consumer。
- ContentItem normalization、URL canonicalization、内容 hash 生成或 Deduplication。
- Event clustering、Event/Evidence、Importance Scoring。
- AI summary、AI analysis、Market Validation、Research Recommendation。
- Portfolio、Holding、Investment Plan、Candidate Rule。
- 自动交易或券商/交易所执行接口。
- 绕过登录、付费墙、captcha、robots、访问控制、限流或 provider 条款。
- Bloomberg / WSJ 未授权全文采集。
- provider SDK、专有 parser 或 raw payload 字段侵入 downstream。
- 新表、新字段、新 enum、新索引、新外键、ORM 修改或 Alembic revision。
- 新依赖；若选定 provider 确实需要依赖，必须先在文档 Review 中单独批准，不能由本 Draft
  自动授权。
- Internal Event Bus 或 Unified News Record schema 实现。
- 修改 Foundation v2.1-FROZEN。

## 7. Source Contract

首个 provider 已由用户确认为 GDELT。下表中无法在不访问外部系统的文档修订中核实的字段
明确标记为 `Pending verification before implementation`；它们必须在 implementation 获准
前用真实、可引用证据补齐，不得猜测。

| Contract Field | 必须记录的内容 | 当前状态 |
|---|---|---|
| `provider` | 供应商/承载方，不等同于原始发布者 | `GDELT` — user selected pilot candidate |
| `source_id` / source code | 既有 Source 标识与稳定 code | 建议 code：`gdelt`；数据库 UUID 在实现配置时生成 |
| `source_type` | 既有 Phase 1 SourceType | `api` |
| `ingestion_mode` | 固定为 `polling` | `polling` |
| realtime tier | Near Real-time / Standard / Periodic | `Standard`；保守目标，不代表 GDELT SLA |
| endpoint / feed / public page | 精确、合法、非秘密接入点 | Pending verification before implementation；本次不请求 endpoint |
| authentication requirement | none / API key / approved subscription 等 | Pending verification before implementation；likely public，但必须核验 |
| `access_level` | PUBLIC_FULLTEXT / PUBLIC_SUMMARY / SUBSCRIPTION_REQUIRED / LICENSED / LINK_ONLY / BLOCKED | 默认 `LINK_ONLY`；只有核验明确允许公开摘要时才可用 `PUBLIC_SUMMARY`；不得用 `PUBLIC_FULLTEXT` |
| `license_policy` | 获取、保存、处理、展示、再分发限制 | Pending verification before implementation；未授权全文不得保存 |
| robots / terms | 检查依据、检查日期和限制 | Pending verification before implementation |
| content retention policy | 可保存字段、保留期限和最小化规则 | 默认最小化为 title/metadata/link；具体期限 Pending verification before implementation |
| allowed content scope | full text / summary / title / link only | title / metadata / link / 经验证允许的 public summary；全文 fail closed |
| canonical URL | 是否提供及来源原始规则；本 SPEC 不做 downstream canonicalization | 使用 provider 返回或原始来源 URL；可用性 Pending verification；下游 canonicalization 属于 SPEC-0006 |
| stable external ID | provider ID 或合法确定性 fallback；不得编造 | 优先 provider event/article ID；若无则使用 source-scoped deterministic fallback，规则须在实现前验证 |
| `published_at` | 来源字段、时区、缺失规则 | 按经验证的 provider 字段映射；缺失为 null，不猜测 |
| `source_updated_at` | 来源字段、时区、缺失规则 | 按经验证的 provider 字段映射；缺失为 null，不猜测 |
| `first_seen_at` / `fetched_at` | 系统时间映射，不覆盖来源时间 | existing framework |
| cursor / watermark | 类型、值、单调性、初始值和 checkpoint 规则 | 时间窗口或 provider 支持的分页/cursor；必须单调、可恢复；精确字段 Pending verification before implementation |
| sequence number | provider 若提供则记录；否则 null | 未验证；不提供时为 null |
| polling interval | 与条款、rate limit 和 realtime tier 一致 | Pending verification before implementation；保守设置并遵守 rate limit |
| rate limit | 限额、窗口、429 与 Retry-After 规则 | Pending verification before implementation；不得猜测 |
| timeout | connect/read/total deadline | Pending verification before implementation；不得猜测 |
| retry / backoff | FR-05 分类与 SPEC-0003 policy 映射 | Pending verification before implementation；复用 SPEC-0003 上限，不猜 provider 参数 |
| backfill window | 允许范围、分页、最大窗口；不批准通用框架 | 只允许经验证的有限时间窗回补；精确上限 Pending verification；不实现通用 Historical Backfill |
| idempotency key | `source_id + stable external_id` 优先，fallback 必须确定且可解释 | `source_id + stable external ID` 优先；fallback 必须 source-scoped、确定且可测试 |
| duplicate protection | adapter/RawItem 层防重复获取规则；不等于 ContentItem dedup | 仅 RawItem/collection 层；ContentItem dedup 属于 SPEC-0006 |
| failure classification | provider error 到稳定 collection error 的映射 | Pending verification before implementation；必须映射到 SPEC-0003 stable errors |
| raw payload retention | retention class、payload_location/hash 和禁止保存内容 | 只保存许可允许的最小 payload 或引用；不保存受限全文 |
| test fixtures | 合法、脱敏、最小、无秘密 fixture 来源与版本 | 必须合法、脱敏、无秘密且不含受限全文；具体 fixture Pending verification |
| verification evidence | 授权证据、合同测试、smoke test 日期和真实结果 | Pending verification before implementation |

映射要求：

- `access_level` 和 `license_policy` 是来源合同元数据；在不改 schema 的前提下，只能使用既有
  `SourceAccount.collection_options` 的版本化、非秘密配置及 `RawItem.retention_class` /
  `payload_location` 合法映射。
- 标记为 Pending verification 的必需合同字段是 implementation blocker，不得通过增加 schema
  或假值绕过。
- secrets 只通过环境或秘密存储引用，绝不写入 `collection_options`、fixture、日志或文档。

## 8. 功能需求

### FR-01 — Approved Single Polling Source

要求：实现只允许一个由用户明确批准的合法 Polling Source。Source 必须 enabled，
`authorization_status` 为 `authorized` 或 `implemented`，且使用本 SPEC 注册的 adapter key。

验收方式：配置和调度测试证明只有获批 Source/SourceAccount 可运行；其他 provider、未知 key、
未授权状态和第二来源均 fail closed，不产生 CollectionRun 或网络请求。

### FR-02 — Provider-Neutral Adapter Boundary

要求：Source Adapter / Connector 负责认证读取、provider-specific mapping 和错误翻译。
它只能返回统一 `RawItemEnvelope` / `FetchBatch`，不得写数据库、推进 cursor、调度 retry、
发送通知或调用 downstream。

Downstream 不得导入 provider SDK，不得读取 provider raw payload 专有字段。provider parser
不得扩散到 normalization、dedup、outbox、Event 或 AI。

验收方式：adapter contract test、依赖扫描和模块边界测试证明 provider 代码只存在于 adapter
边界，runner 与 downstream 只依赖内部 contract。

### FR-03 — Access and License Enforcement

要求：每次采集使用审核通过的 `access_level`、`license_policy`、retention class 和 allowed
content scope。公开网页不等于允许批量保存全文：

- `PUBLIC_FULLTEXT` 只有在明确允许时保存全文；
- `PUBLIC_SUMMARY` 只保存公开摘要及允许元数据；
- `SUBSCRIPTION_REQUIRED`、`LINK_ONLY`、`BLOCKED` 可作为线索状态，但不得伪装为 full text；
- `LICENSED` 只按合同允许范围保存和处理；
- Bloomberg / WSJ 未授权时，完整正文采集不是成功条件；
- 不绕过付费墙、登录、captcha、robots 或访问控制；
- 付费或受版权保护内容只保存授权范围内容或引用。

验收方式：fixtures 覆盖全部 access level 映射；安全测试证明受限内容不会进入 payload，禁止
路径不会发起 fallback。

### FR-04 — Cursor, Watermark and Limited Backfill

要求：adapter 明确 cursor/watermark 类型、初始状态、单调 successor 和时间水位。所有
checkpoint 继续使用 SPEC-0003 的 RawItem/cursor 原子事务。

仅允许 provider 合同明确支持、用户批准且有固定上限的 backfill window；无法直接恢复时
必须可见失败，不得自动扩大时间窗、切换 provider 或实现通用 Historical Backfill 框架。

验收方式：cursor resume、分页 checkpoint、旧 watermark、rollback、重启恢复和受限 backfill
测试；失败时 cursor 不推进。

### FR-05 — Retry, Rate Limit and Failure Visibility

要求：provider-specific 错误映射到 SPEC-0003 的稳定错误分类，统一框架负责 retry/backoff。
429 使用合法 Retry-After；auth/forbidden/not-found/contract/config 错误不得盲目 retry。
所有错误必须脱敏且在 CollectionRun 可见。

验收方式：fixture/transport fault injection 覆盖 timeout、network、429、5xx、auth、forbidden、
not found、invalid payload 和未知异常；断言 run 状态、retry 决策和日志脱敏。

### FR-06 — Idempotency and RawItem Persistence

要求：stable external ID 优先形成确定性幂等身份；provider 无稳定 ID 时，fallback 必须在
文档中定义为来源范围内稳定、可测试且不依赖随机值。RawItem 必须关联当前 CollectionRun，
且只保存许可允许的最小 payload 或引用。

本 SPEC 的 duplicate protection 只保证重复 poll/retry 不产生不受控副作用；ContentItem
normalization 与 dedup 仍属于 SPEC-0006。

验收方式：重复 poll、retry、cursor replay 和有限 backfill 测试证明幂等，RawItem 来源/run/
account 关联正确，无孤立数据。

### FR-07 — Time Semantics

要求：严格区分来源 `published_at`、来源 `source_updated_at`、系统 `first_seen_at` 和本次
`fetched_at`。缺失值保持 null，不猜测；provider 时区必须明确并转换为 UTC。

验收方式：fixture 覆盖完整时间、缺失时间、非 UTC offset、更新记录和重复获取。

### FR-08 — Verification Evidence Gate

要求：真实 smoke test 只能在用户确认 provider、授权与接入政策后运行。结果必须记录日期、
endpoint 类别、授权状态、内容范围、请求数、RawItem/CollectionRun/cursor 结果和任何限制；
不得记录秘密或未授权正文。

验收方式：实施 PR 的 Delivery Report 提供真实证据；未授权或无法访问时必须明确 BLOCKED，
只能报告 contract/fixture 测试，不能声称真实接入通过。

## 9. 非功能需求

- 可靠性：重复 poll、retry 和重启保持幂等；cursor 只在 RawItem 安全持久化后推进。
- 性能：轮询间隔、batch 和 backfill 有界，遵守 provider rate limit。
- 安全：外部数据不可信；无登录/付费墙/captcha 绕过；秘密、cursor、query 和 payload 脱敏。
- 合规：access/license/retention 决定可获取与保存范围；blocked 必须 fail closed。
- 可替换性：删除该 adapter 不改变 runner 或 downstream contract。
- 可观测性：每次真实运行有 CollectionRun，失败不能表现为“没有新消息”。
- 实时性：只声明审核后的 Near Real-time、Standard 或 Periodic tier，不承诺 provider 未保证的 SLA。

## 10. 数据模型变化

无。

- 不创建 Alembic revision。
- 不新增或修改表、字段、enum、索引、外键或约束。
- 不修改现有 ORM 模型。
- 只使用既有 `Source`、`SourceAccount`、`CollectionCursor`、`CollectionRun` 和 `RawItem`。
- `access_level` / `license_policy` 使用第 7 节定义的现有 JSONB/retention 映射，不创建新字段。
- 实现验收必须证明 PostgreSQL schema 无 drift。

## 11. 接口与任务变化

- 新增：一个 provider-specific `CollectionAdapter` 实现及其 adapter key 注册。
- 复用：`CollectionTarget`、`CursorSnapshot`、`FetchRequest`、`RawItemEnvelope`、
  `FetchBatch`、`ClassifiedCollectionError`。
- 复用任务：`collection.dispatch_due_targets`、`collection.run_target`、
  `collection.recover_stale_runs`。
- API：无。
- CLI：无。
- Bot：无。
- Outbox / Notification：无行为变化。
- 新业务消息或 Internal Event Bus：无。

## 12. 配置变化

未来实现只允许增加该 provider 所需的无秘密配置模板和秘密引用名称。精确配置必须在 provider
确认后写入本文档，包括 endpoint、poll interval、timeout、batch limit、backfill limit 和
credential reference。

- 不提交真实 Token、Cookie、账号、订阅凭据或 `.env`。
- 不把秘密放入 Source/SourceAccount、fixture、日志或 Delivery Report。
- GDELT 的精确配置保持 `Pending verification before implementation`；不得预先添加其他
  provider 配置。

## 13. 错误处理

| Error Code | 条件 | Retry | 运维表现 |
|---|---|---|---|
| `COLLECTION_TIMEOUT` | provider 请求超时 | 是 | 统一 backoff，cursor 不推进 |
| `COLLECTION_NETWORK` | 临时网络失败 | 是 | 统一 backoff |
| `COLLECTION_RATE_LIMITED` | 429/明确限流 | 是 | 合法 Retry-After 或 capped backoff |
| `COLLECTION_UPSTREAM_5XX` | provider 5xx | 是 | 统一 backoff |
| `COLLECTION_UPSTREAM_RETRYABLE` | contract 明确的临时错误 | 是 | 统一 backoff |
| `COLLECTION_AUTH` | 缺失或失效授权 | 否 | failed；等待人工确认 |
| `COLLECTION_FORBIDDEN` | 401/403、robots/terms/access 阻塞 | 否 | failed/blocked；不得 fallback |
| `COLLECTION_NOT_FOUND` | endpoint/feed 404/410 | 否 | failed；人工核验 |
| `COLLECTION_CONTRACT_INVALID` | payload/cursor/envelope 不符合合同 | 否 | failed；开发错误可见 |
| `COLLECTION_CONFIG_INVALID` | provider 未确认、adapter 未注册或配置缺失 | 否 | 不运行或 failed |
| `COLLECTION_UNKNOWN` | 未分类异常 | 否 | failed；不得盲目 retry |

其余数据库、lock、cancelled、stale-run 分类沿用 SPEC-0003。错误正文必须长度受限并脱敏。

## 14. Migration and Rollback

- 升级：无数据库迁移；未来实现只增加一个可移除 adapter、注册、fixture、配置模板和测试。
- 回滚：先禁用对应 Source/SourceAccount 并停止其任务，再移除 adapter 注册和配置；既有
  RawItem、CollectionRun 和 cursor 保留用于审计。
- 数据兼容：只写既有 Phase 1 表和字段，不改变 schema。
- 备份要求：无新 schema；真实或受限 payload 不得进入 Git、测试 fixture 或 Review ZIP。

## 15. Tasks

所有任务在本文档 Review PASS 且用户确认 provider 前保持未开始：

- [ ] T01 — 用户确认唯一试点 provider、Source/SourceAccount、合法接入与内容范围。
- [ ] T02 — 将第 7 节 Source Contract 的全部 Pending verification 项更新为已验证值并再次审核。
- [ ] T03 — 实现一个 provider-specific Polling Adapter/Connector 和唯一 adapter key。
- [ ] T04 — 映射统一 envelope、时间、cursor/watermark、access/license 和 retention。
- [ ] T05 — 实现 provider error mapping、rate limit、retry 和有限 backfill。
- [ ] T06 — 增加 fixture-based contract、cursor、幂等、访问政策和故障注入测试。
- [ ] T07 — 增加 PostgreSQL/Redis/Celery 集成与 no-schema-drift 验证。
- [ ] T08 — 授权确认后执行一次有界 real-source smoke test，并记录真实证据。
- [ ] T09 — 更新 Source Catalog、配置模板、Changelog 和 Delivery Report。

## 16. 测试要求

本轮文档准备不写测试代码。未来实现至少需要：

- Provider-neutral adapter contract test。
- Fixture 驱动且禁用外部网络的单元测试。
- 仅在授权确认后的有界 real-source smoke test。
- Cursor resume / watermark / checkpoint 原子性测试。
- Retry、Retry-After、backoff 和错误分类测试。
- Idempotency 与 duplicate protection 测试。
- `access_level` / `license_policy` mapping 和 retention 测试。
- RawItem persistence、CollectionRun 生命周期和 source/account 关联测试。
- PostgreSQL schema drift / Alembic check，证明无迁移。
- downstream 无 provider SDK、provider module 或 raw payload 专有字段依赖的边界测试。
- 禁止 paywall/login/captcha/robots/access-control bypass 的安全测试。
- failure visibility、日志脱敏和 blocked/fail-closed 测试。
- provider 未确认、未授权、未知 adapter 和第二来源不产生网络请求的测试。

## 17. 验收标准

- [ ] 只接入一个由用户批准的合法 Polling Source。
- [ ] 首个 provider 已确认为 GDELT；endpoint、授权与内容范围已完成真实核验，不再处于 Pending。
- [ ] provider 可替换，且未成为 framework 或 downstream 的核心依赖。
- [ ] Source Adapter / Connector 将 provider 数据转换为统一内部 envelope。
- [ ] downstream 不导入 provider SDK，不读取 raw payload 专有字段。
- [ ] RawItem 可真实写入并正确关联 CollectionRun、Source 和 SourceAccount。
- [ ] CollectionRun 状态、计数、错误分类和重试正确。
- [ ] cursor / watermark 可安全恢复，只在持久化后推进。
- [ ] 重复 poll、retry 和有限 backfill 保持幂等。
- [ ] `access_level`、`license_policy` 和 retention 可追踪且被执行。
- [ ] 没有绕过付费墙、登录、captcha、robots、访问控制或 provider 限制。
- [ ] 未授权 Bloomberg / WSJ 全文不是成功条件，也未被采集。
- [ ] 未新增 schema、ORM 变化或 Alembic revision。
- [ ] 未实现 ContentItem normalization、Deduplication、Notification 或 Outbox 行为。
- [ ] 未实现 Streaming、Webhook、X、Telegram、Event、AI、Market Validation 或 Research Recommendation。
- [ ] 所有验证命令和 real-source smoke test 结果真实记录；未运行不得写成 PASS。
- [ ] `git diff --check`、Foundation validation、静态/测试/schema/runtime 验证按实现风险通过。

## 18. Verification Evidence

| Task/Requirement | 命令或证据 | 结果 | 日期 |
|---|---|---|---|
| 文档准备：diff | `git diff --check` | PASS | 2026-07-29 |
| 文档准备：Foundation | `python3 scripts/validate-foundation.py` | PASS（Required files: 20；Markdown links: valid；Freeze markers: valid） | 2026-07-29 |
| Provider confirmation | 用户明确选择 GDELT 作为首个 Polling Source Pilot candidate | PASS（仅 provider selection；不代表授权或实现批准） | 2026-07-29 |
| Provider contract verification | endpoint、认证、许可、terms/robots、rate limit、timeout 与真实证据 | Pending verification before implementation | |
| Implementation | 第 16–17 节要求 | Not started | |

## 19. Commit Evidence

| 类型 | Commit |
|---|---|
| Specification activation | 由本 Draft PR 提交记录证明 |
| Implementation | 无；implementation not started |
| Review fixes | 无 |

## 20. 实现结果

未开始。本 PR 只准备和激活 SPEC-0004 文档，并记录用户选择 GDELT candidate；没有接入或
请求真实来源。

## 21. 与 SPEC 的偏差

无。

## 22. 已知问题与风险

- Blocker：GDELT 已被选为首个 provider candidate，但具体 endpoint/feed、授权状态、许可政策、
  terms/robots、保存范围、rate limit、timeout、cursor 字段和真实验证证据仍待实现前核验；
  核验完成且本文档 Review PASS 前不得实现。
- Must Fix：进入 implementation 前补齐第 7 节所有 `Pending verification before implementation`。
- Improvement：无。
- Future Scope：第二 provider、Streaming、Webhook、通用 Historical Backfill、X、
  Normalization/Dedup/Outbox、Telegram、Event、AI、Market Validation、Research
  Recommendation 均属于其他 SPEC。

## 23. Review History

| 轮次 | 结果 | 主要问题 | 处理 |
|---|---|---|---|
| 0 | Pending | 初始文档准备；provider 尚待用户确认 | 未开始实现 |
| 1 | REQUEST CHANGES | 首个 provider 未确认，Source Contract 存在实现必需未决项 | 用户选择 GDELT；未核验事项明确标为 implementation blocker |

## 24. 架构与治理检查

- Active Foundation 版本：v2.1-FROZEN。
- 是否依赖 Proposed Decision：仅引用 provider-neutral 方向作为接口约束，不启用 D-020–D-024
  的新基础设施或未来阶段能力。
- 是否需要先完成 Freeze Review：本 SPEC 的单一合法 Polling Source 与 Phase 1 一致，不改变
  Foundation；Event/AI/Market Validation/Research Recommendation 等禁止项仍需未来流程。
- 是否改变阶段边界、schema 或既有实体：否。
- 外部 Provider / Connector 及授权状态：GDELT Polling candidate 已选择；授权、endpoint 与
  合同细节 Pending verification before implementation。
- 数据许可、保存与再分发限制：必须在第 7 节逐项确认；默认最小化、fail closed。
- 采集模式：只允许 Polling。
- cursor、sequence、ack、retry 与 recovery：使用 SPEC-0003 contract；sequence 若无则 null，
  有限 backfill 由 provider 合同限定。
- 是否保持未知或未授权能力 fail closed：是。
