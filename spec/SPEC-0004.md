# SPEC-0004 — First Approved Polling Source Pilot

状态：Active — Docs Review（implementation not started）
阶段：Phase 1 — Information Collection & Push
负责人：Project Owner
创建日期：2026-07-29
最后更新：2026-07-29

## 1. 目标

定义第一个合法、低成本或公开 Polling Source Pilot 的实现合同，为后续真实来源接入提供
provider-neutral 模板。本 SPEC 的文档进入审核不代表实现授权。用户已选择 GDELT 作为唯一
试点 candidate；这不表示 endpoint、授权、条款或运行参数已经验证。本文档 Review PASS 且
所有 implementation 前核验完成后，才可另行授权实现。

未来实现必须验证：

- SPEC-0003 collection framework 能接入一个经批准的真实 polling source；
- 既有 `Source`、`SourceAccount`、`CollectionCursor`、`CollectionRun`、`RawItem` 能正确
  承载真实来源运行；
- cursor、watermark、retry、idempotency、有限 backfill 和 access policy 合同可执行；
- downstream 只依赖统一内部 envelope，不依赖 provider SDK 或 raw payload；
- 无 schema 变化，且不实现 downstream normalization、dedup、outbox、AI 或 Event。

## 2. 背景

SPEC-0003 已完成并通过 fake adapter 验证来源无关采集框架。下一步只能选择一个合法来源，
以最小范围证明真实 Polling 接入，同时保持 `Source Adapter / Connector → 统一 envelope →
现有 collection runner` 边界。

PR #6 记录了长期 provider-neutral 架构方向。SPEC-0004 只采用与当前 Phase 1 一致的
Polling adapter 边界，不批准 Streaming、Webhook、通用 Historical Backfill、Internal
Event Bus、Event、AI、Market Validation 或 Research Recommendation。

## 3. Foundation 与阶段边界

- 使用的冻结 Decision：D-003、D-006、D-009、D-010、D-012、D-014、D-015、D-018、
  D-019，以及 Foundation v2.1-FROZEN 的 Content First、合法授权、Failure Visibility。
- 当前阶段允许：一个经用户批准的合法 Polling Source、provider-specific adapter mapping、
  RawItem 留痕、CollectionRun、cursor/watermark、retry、幂等与有限回补。
- 当前阶段禁止：AI/LLM、Event、Evidence、Analysis、Portfolio、Holding、Investment Plan、
  Candidate Rule、Market Validation、Research Recommendation、交易建议和自动交易。
- D-020–D-024 仍是 Proposed Decisions；本 SPEC 不把它们变为 Foundation 生效事实。
- 是否修改 Foundation：否。
- 是否修改 schema：否。
- 是否开始实现：否；首个 provider candidate 已确认为 GDELT，但本文档必须先 Review PASS，
  且第 7 节所有 implementation blocker 必须完成真实核验。

## 4. 前置条件与依赖

- 前置 SPEC：SPEC-0003 已完成并合并，tag `spec-0003-completed`。
- 当前活动：GDELT bounded smoke verification 已完成并记录；implementation still not
  started。
- 基础设施：SPEC-0001–0003 的 Python/PostgreSQL/Redis/Celery、九个 Phase 1 业务表、
  CollectionAdapter contract、AdapterRegistry、scheduler、runner、lock 和 retry framework。
- 外部权限：GDELT candidate 已由用户确认；本轮获准对 DOC API 做最多三次有界 smoke，
  实际尝试两次且没有取得文章数据。没有下载数据或访问任何新闻来源页面。
- 实现前用户确认事项：
  1. 唯一试点 provider：GDELT（已确认）；
  2. endpoint family：DOC 2.0 API `ArticleList` JSON（文档核验 selected；bounded smoke
     已观察到 endpoint 的 429 响应）；
  3. access level：`LINK_ONLY`；只保留最小 metadata/source URL，不保存新闻正文；
  4. 尚待核验：成功响应、实际无 API key 行为、numeric rate limit、Retry-After、精确响应
     schema、长期可用性和 retention duration。

### 4.1 Provider Selection Gate

用户已选择 GDELT 作为 SPEC-0004 唯一 Polling Source Pilot candidate。这一选择只确定
provider，不证明授权、endpoint、可用性、许可、实时等级或内容范围，也不使 GDELT 成为
核心依赖。

RSS / Atom、CNBC、Reuters、公司 IR、SEC/政府机构、NewsAPI.ai / Event Registry、Finnhub
及其他来源均不属于本 SPEC，不得作为 fallback 或第二 provider 接入。

选择规则：

- 具体第一个 provider 已由用户明确确认为 GDELT；
- GDELT implementation 仍不得开始，直到本文档 Review PASS 且第 7 节待核验项完成；
- GDELT 必须可替换，不得成为 framework 或 downstream 的核心依赖；
- 未授权、未知、阻塞或合同不完整的能力必须 fail closed；
- 不得从一个失败接入方式 fallback 到网页抓取、登录绕过或其他未批准方式；
- 只允许一个试点来源，禁止顺带接入第二个 provider。

## 5. 范围

- 为用户选择的 GDELT Polling Source 定义并在后续获准后实现一个 provider-specific
  Adapter/Connector。
- 将 provider 响应映射为 SPEC-0003 的统一 `RawItemEnvelope` / `FetchBatch` 合同。
- 使用既有 Source Registry、scheduler、CollectionRun、RawItem persistence、Redis lock、
  retry 和 cursor checkpoint。
- 使用既有 `Source.access_method` 注册唯一 adapter key；未知 key fail closed。
- 在既有 `SourceAccount.collection_options` 中承载经审核的非秘密来源合同配置，包括
  access/license、cursor、水位、轮询和有限 backfill 规则；不得新增字段。
- 根据许可仅保存允许范围的 payload、摘要、标题、链接、元数据、hash 或受控引用。
- 增加未来实现所需的 fixture、contract、集成和授权后 smoke test。
- 更新来源配置模板、Source Catalog 状态、Changelog 和 Delivery Report，仅限未来实现 PR。

## 6. 非范围

- Streaming Source、WebSocket、Kafka 或商业实时流。
- Webhook Source。
- Historical Backfill 的完整通用框架；只允许被选 provider 明确支持且经批准的有限时间窗
  或 cursor 回补。
- X Source 或任何 X 账号。
- Telegram Bot 或 Telegram 投递。
- Notification 策略、Outbox publisher/consumer。
- ContentItem normalization、URL canonicalization、内容 hash 生成或 Deduplication。
- Event clustering、Event/Evidence、Importance Scoring。
- AI summary、AI analysis、Market Validation、Research Recommendation。
- Portfolio、Holding、Investment Plan、Candidate Rule。
- 自动交易或券商/交易所执行接口。
- 绕过登录、付费墙、captcha、robots、访问控制、限流或 provider 条款。
- Bloomberg / WSJ 未授权全文采集。
- provider SDK、专有 parser 或 raw payload 字段侵入 downstream。
- 新表、新字段、新 enum、新索引、新外键、ORM 修改或 Alembic revision。
- 新依赖；若选定 provider 确实需要依赖，必须先在文档 Review 中单独批准，不能由本 Draft
  自动授权。
- Internal Event Bus 或 Unified News Record schema 实现。
- 修改 Foundation v2.1-FROZEN。

## 7. Source Contract

首个 provider 已由用户确认为 GDELT。本轮有界 smoke 的真实结果为一次 429 和一次 SSL
connection timeout；未得到成功响应或文章数据。仍无法核实的字段明确标记为
`Pending verification before implementation` 或 Blocked，不得猜测。

| Contract Field | 必须记录的内容 | 当前状态 |
|---|---|---|
| `provider` | 供应商/承载方，不等同于原始发布者 | `GDELT` — user selected pilot candidate |
| `source_id` / source code | 既有 Source 标识与稳定 code | 建议 code：`gdelt`；数据库 UUID 在实现配置时生成 |
| `source_type` | 既有 Phase 1 SourceType | `api` |
| `ingestion_mode` | 固定为 `polling` | `polling` |
| realtime tier | Near Real-time / Standard / Periodic | `Standard`；15 分钟级保守 polling 设计，不是 GDELT SLA |
| endpoint / feed / public page | 精确、合法、非秘密接入点 | Verified reachable：官方 DOC 2.0 endpoint 对无凭据请求返回 HTTP 429；未验证成功可用性 |
| authentication requirement | none / API key / approved subscription 等 | 请求未携带 API key，但在成功响应前被限流；实际无需 API key 仍 Blocked |
| `access_level` | PUBLIC_FULLTEXT / PUBLIC_SUMMARY / SUBSCRIPTION_REQUIRED / LICENSED / LINK_ONLY / BLOCKED | Verified/constrained：`LINK_ONLY`；不得使用 `PUBLIC_FULLTEXT` |
| `license_policy` | 获取、保存、处理、展示、再分发限制 | GDELT 数据集可免费、不限用途使用/再分发，但必须引用 GDELT 并链接官网；第三方新闻内容权利不随 GDELT 许可授予 |
| robots / terms | 检查依据、检查日期和限制 | GDELT Terms verified；API-specific robots/自动化频率未单独说明，必须遵守 rate limiting 且不访问 source pages |
| content retention policy | 可保存字段、保留期限和最小化规则 | Constrained：只保存 title、metadata、source URL、timestamps、provider/deterministic ID、payload hash/reference；保留期限仍 Pending policy approval |
| allowed content scope | full text / summary / title / link only | title / metadata / source link；可保留 GDELT 返回的 publisher description metadata，但不得抓取或保存正文 |
| canonical URL | 是否提供及来源原始规则；本 SPEC 不做 downstream canonicalization | ArticleList/RSS 文档确认返回 article URL；原样保存 source URL，下游 canonicalization 属于 SPEC-0006 |
| stable external ID | provider ID 或合法确定性 fallback；不得编造 | DOC ArticleList 未承诺稳定 article ID；selected fallback：`sha256(source_id + exact returned URL)` |
| `published_at` | 来源字段、时区、缺失规则 | ArticleList 文档显示 publication date；精确 JSON 字段/语义须 fixture/smoke 验证，无法确认时为 null |
| `source_updated_at` | 来源字段、时区、缺失规则 | 官方 DOC ArticleList 文档未确认 updated timestamp；固定为 null，不猜测 |
| `first_seen_at` / `fetched_at` | 系统时间映射，不覆盖来源时间 | existing framework |
| cursor / watermark | 类型、值、单调性、初始值和 checkpoint 规则 | Selected：UTC closed-open windows using `STARTDATETIME`/`ENDDATETIME`；完整窗口持久化后推进 end watermark，并以受控 overlap 重读 |
| sequence number | provider 若提供则记录；否则 null | DOC ArticleList 未文档化 sequence；固定为 null |
| polling interval | 与条款、rate limit 和 realtime tier 一致 | Selected initial policy：每个批准 query 最快每 15 分钟一次、串行且无 burst；真实限流验证前不得加快 |
| rate limit | 限额、窗口、429 与 Retry-After 规则 | Runtime verified：一次 HTTP 429；该响应未观察到 `Retry-After`，numeric quota/window 仍 Blocked |
| timeout | connect/read/total deadline | 一次尝试在 30 秒 curl 总时限内发生 SSL connection timeout；provider 建议值仍 Pending，不得从本次网络环境推断 |
| retry / backoff | FR-05 分类与 SPEC-0003 policy 映射 | Constrained：复用 SPEC-0003 capped full-jitter；429/5xx/network/timeout 可重试，未公布 provider 参数不得猜 |
| backfill window | 允许范围、分页、最大窗口；不批准通用框架 | Verified capability：DOC supports `TIMESPAN` and precise start/end within documented rolling window；只允许 bounded replay |
| idempotency key | `source_id + stable external_id` 优先，fallback 必须确定且可解释 | Selected：`source_id + sha256(exact returned URL)`；若 smoke 证明有稳定 provider ID，再优先使用该 ID |
| duplicate protection | adapter/RawItem 层防重复获取规则；不等于 ContentItem dedup | overlap 按 selected key 抑制副作用；无通用 cursor/offset，250 条封顶窗口必须缩小或 fail visible；ContentItem dedup 属于 SPEC-0006 |
| failure classification | provider error 到稳定 collection error 的映射 | Runtime evidence：429 → rate limited；SSL connection timeout → network/timeout；其余 status/body 仍 Pending |
| raw payload retention | retention class、payload_location/hash 和禁止保存内容 | 只保存许可允许的最小 per-item metadata 或受控引用/hash；禁止新闻正文、页面 HTML、图片和完整 API response |
| test fixtures | 合法、脱敏、最小、无秘密 fixture 来源与版本 | 未来只用合成/最小字段 fixture；真实 fixture 获取仍 Blocked pending implementation authorization |
| verification evidence | 授权证据、合同测试、smoke test 日期和真实结果 | 官方文档和两次 bounded smoke attempt 已记录；未运行 collection，成功响应 evidence 仍 Blocked |

映射要求：

- `access_level` 和 `license_policy` 是来源合同元数据；在不改 schema 的前提下，只能使用既有
  `SourceAccount.collection_options` 的版本化、非秘密配置及 `RawItem.retention_class` /
  `payload_location` 合法映射。
- 标记为 Pending verification 的必需合同字段是 implementation blocker，不得通过增加 schema
  或假值绕过。
- secrets 只通过环境或秘密存储引用，绝不写入 `collection_options`、fixture、日志或文档。

### 7.1 GDELT Verification Evidence

官方文档核验与本轮有界 smoke 证据均记录在此。smoke 只请求获准的 DOC 2.0 endpoint，
没有下载数据集、访问新闻来源页面、运行 collection 或保存 API response / 真实 GDELT 数据。
官方文档结论与第三方新闻内容授权严格分开。

接口产品边界：

- 本 SPEC-0004 只选择 GDELT Project 的 legacy / public DOC 2.0 `ArticleList` endpoint family：
  `https://api.gdeltproject.org/api/v2/doc/doc`。
- 本 SPEC 不使用 GDELT Cloud Developer API（`https://gdeltcloud.com/api/v2/*`）。
- GDELT Cloud 的 API key、quota、cursor pagination、Events / Stories / Entities、商业计划和
  Acceptable Use 均不属于本 SPEC。
- 不得把 GDELT Cloud 的认证、分页、rate limit、license 或 redistribution 条款混用于
  GDELT Project DOC 2.0 contract。
- 如果未来决定使用 GDELT Cloud，必须创建独立 SPEC 或修订 SPEC-0004，并重新核验授权、
  价格、API key、terms、rate limit、pagination 和数据使用限制。

| 项目 | 核验结论 | 来源 | 是否 implementation blocker | 剩余风险 |
|---|---|---|---|---|
| 可用接口 | GDELT 提供 DOC、GEO、Context 等 API 以及批量数据；本 SPEC 只选择 DOC 2.0 `ArticleList` JSON endpoint family | [DOC 2.0 API](https://blog.gdeltproject.org/gdelt-doc-2-0-api-debuts/) | 否 | 其他接口不在本 SPEC 范围 |
| endpoint | 官方示例使用 `https://api.gdeltproject.org/api/v2/doc/doc`，`mode=artlist`、`format=json` | [DOC 2.0 API](https://blog.gdeltproject.org/gdelt-doc-2-0-api-debuts/) | 是：须获准后 smoke 验证 | 未验证当前可用性、响应 schema 和错误响应 |
| 认证 | 官方 DOC 参数说明未列 API key 或认证参数 | [DOC 2.0 API](https://blog.gdeltproject.org/gdelt-doc-2-0-api-debuts/) | 是：须获准后 smoke 验证 | 不把“未列出”扩大为永久无认证保证 |
| 时间窗口 | 支持 `TIMESPAN`，以及 UTC `STARTDATETIME` / `ENDDATETIME`；文档描述可查询最近三个月 | [DOC 2.0 API](https://blog.gdeltproject.org/gdelt-doc-2-0-api-debuts/) | 否 | rolling window 的当前边界仍须 smoke 验证 |
| 记录上限与恢复 | `ArticleList` 默认 75 条，`MAXRECORDS` 最大 250；官方参数未文档化 cursor、page 或 offset | [DOC 2.0 API](https://blog.gdeltproject.org/gdelt-doc-2-0-api-debuts/) | 否 | 达到上限时必须缩小窗口或 fail visible，不能假定完整 |
| 更新频率 | GDELT 2.0 Event/GKG 官方说明约每 15 分钟更新；本 SPEC 仅把 15 分钟作为保守轮询下限，不视为 DOC API SLA | [GDELT 2.0](https://blog.gdeltproject.org/gdelt-2-0-our-global-world-in-realtime/) | 否 | DOC API 没有已核验 SLA |
| rate limit | 官方明确 DOC/Context API 实施 rate limiting，但未公布数字配额 | [API Rate Limiting](https://blog.gdeltproject.org/ukraine-api-rate-limiting-web-ngrams-3-0/) | 是 | 数值限额、窗口和 `Retry-After` 行为未知 |
| timeout / retry | 未在已查阅官方 DOC 文档中找到明确 timeout 或 provider retry 数值建议 | [DOC 2.0 API](https://blog.gdeltproject.org/gdelt-doc-2-0-api-debuts/) | 是 | 实现不得猜测 provider 数值；须有界 smoke/运维确认 |
| 返回内容 | `ArticleList` 文档说明文章列表包含 title、source country、language、publication date，并可通过 RSS/JSON feed 返回 URL/title | [DOC 2.0 API](https://blog.gdeltproject.org/gdelt-doc-2-0-api-debuts/) | 是：精确 JSON 字段须验证 | nullable、时区、字段名和 summary 语义未知 |
| stable ID | DOC `ArticleList` 未承诺 article ID；Event 数据的 `GlobalEventID` 只适用于 Event dataset，不能借作 article ID | [Event Codebook](https://data.gdeltproject.org/documentation/GDELT-Event_Codebook-V2.0.pdf) | 否 | 使用 source-scoped exact-URL hash；URL 变化会产生新 observation |
| URL / canonical URL | 文档确认列表输出 article URL；本 SPEC 只原样保存 source URL，不声称 canonical | [DOC 2.0 API](https://blog.gdeltproject.org/gdelt-doc-2-0-api-debuts/) | 是：精确字段须验证 | canonicalization 留给 SPEC-0006 |
| terms / attribution | GDELT 数据集允许免费使用和再分发，但使用或再分发须引用 GDELT 并链接官网 | [GDELT Terms of Use](https://www.gdeltproject.org/about.html#termsofuse) | 否 | 该条款不授予第三方新闻正文、图片或网页内容权利 |
| access / retention | Selected：`LINK_ONLY`；仅保留 title、允许的 metadata、source URL、timestamps、ID/hash/reference | 上述 DOC 文档与 Terms；按最小权限原则约束 | 是：保留期限待批准 | 禁止正文、页面 HTML、图片和完整 API response |
| bounded replay | 时间窗口允许有限重放；不批准通用 Historical Backfill | [DOC 2.0 API](https://blog.gdeltproject.org/gdelt-doc-2-0-api-debuts/) | 否 | 窗口上限与 250 条封顶必须由 contract tests 执行 |

#### Bounded Smoke Verification — 2026-07-29

请求约束：共两次尝试，均为 GDELT Project DOC 2.0 `ArticleList`，使用通用
`query=technology`、`format=json`、`maxrecords=1`、`timespan=15m`；没有请求 GDELT Cloud，
没有跟随或访问 article URL。

| 项目 | 实际观察 | 状态 |
|---|---|---|
| 请求次数 | 2 次尝试；第 1 次收到 HTTP 响应，第 2 次在 SSL 建连阶段超时 | Verified |
| endpoint 可达性 | 第 1 次从目标 endpoint 收到 `HTTP/1.1 429 Too Many Requests` | Partial：可达；成功可用性 Blocked |
| API key | 两次请求均未携带 key；没有获得成功响应 | Pending：不能由 429 推断无需 API key |
| HTTP / redirect | 第 1 次 429；未观察到 `Location`，未跟随 redirect；第 2 次无 HTTP status | Partial |
| content-type / error format | 429 响应未观察到 `Content-Type`，body 不是有效 JSON；未记录 body 内容 | Partial：其他错误格式 Pending |
| JSON / 顶层结构 / article 字段 | 没有成功 JSON，因此顶层结构、字段名、title、URL、publication date 均未 runtime 验证 | Blocked |
| stable article ID / `source_updated_at` | 没有文章对象可检查 | Blocked；沿用官方文档的保守合同 |
| `maxrecords=1` / `timespan=15m` | 参数已发送，但请求未成功执行到可验证结果 | Blocked |
| rate limit / `Retry-After` | 真实观察到 429；该响应未观察到 `Retry-After` | Partial：numeric quota/window 和其他响应仍 Blocked |
| timeout | 第 2 次尝试在 30 秒 curl 总时限内报告 SSL connection timeout | Observed；不等于 provider timeout 建议 |
| robots / terms | 429 响应未观察到 `X-Robots-Tag`；没有访问 source page | API-specific 自动化约束仍 Pending |
| `LINK_ONLY` retention | 未取得或保存文章数据；最小 retention 设计未被本次 smoke 否定 | Constrained；字段支持仍待成功响应 |
| 数据处理边界 | 未保存完整 response、新闻正文、HTML、图片或 source page 内容 | Verified |
| 实现边界 | 未写代码、未实现/注册 adapter、未运行 collection | Verified；implementation not started |

## 8. 功能需求

### FR-01 — Approved Single Polling Source

要求：实现只允许一个由用户明确批准的合法 Polling Source。Source 必须 enabled，
`authorization_status` 为 `authorized` 或 `implemented`，且使用本 SPEC 注册的 adapter key。

验收方式：配置和调度测试证明只有获批 Source/SourceAccount 可运行；其他 provider、未知 key、
未授权状态和第二来源均 fail closed，不产生 CollectionRun 或网络请求。

### FR-02 — Provider-Neutral Adapter Boundary

要求：Source Adapter / Connector 负责认证读取、provider-specific mapping 和错误翻译。
它只能返回统一 `RawItemEnvelope` / `FetchBatch`，不得写数据库、推进 cursor、调度 retry、
发送通知或调用 downstream。

Downstream 不得导入 provider SDK，不得读取 provider raw payload 专有字段。provider parser
不得扩散到 normalization、dedup、outbox、Event 或 AI。

验收方式：adapter contract test、依赖扫描和模块边界测试证明 provider 代码只存在于 adapter
边界，runner 与 downstream 只依赖内部 contract。

### FR-03 — Access and License Enforcement

要求：每次采集使用审核通过的 `access_level`、`license_policy`、retention class 和 allowed
content scope。公开网页不等于允许批量保存全文：

- `PUBLIC_FULLTEXT` 只有在明确允许时保存全文；
- `PUBLIC_SUMMARY` 只保存公开摘要及允许元数据；
- `SUBSCRIPTION_REQUIRED`、`LINK_ONLY`、`BLOCKED` 可作为线索状态，但不得伪装为 full text；
- `LICENSED` 只按合同允许范围保存和处理；
- Bloomberg / WSJ 未授权时，完整正文采集不是成功条件；
- 不绕过付费墙、登录、captcha、robots 或访问控制；
- 付费或受版权保护内容只保存授权范围内容或引用。

验收方式：fixtures 覆盖全部 access level 映射；安全测试证明受限内容不会进入 payload，禁止
路径不会发起 fallback。

### FR-04 — Cursor, Watermark and Limited Backfill

要求：adapter 明确 cursor/watermark 类型、初始状态、单调 successor 和时间水位。所有
checkpoint 继续使用 SPEC-0003 的 RawItem/cursor 原子事务。

仅允许 provider 合同明确支持、用户批准且有固定上限的 backfill window；无法直接恢复时
必须可见失败，不得自动扩大时间窗、切换 provider 或实现通用 Historical Backfill 框架。

验收方式：cursor resume、分页 checkpoint、旧 watermark、rollback、重启恢复和受限 backfill
测试；失败时 cursor 不推进。

### FR-05 — Retry, Rate Limit and Failure Visibility

要求：provider-specific 错误映射到 SPEC-0003 的稳定错误分类，统一框架负责 retry/backoff。
429 使用合法 Retry-After；auth/forbidden/not-found/contract/config 错误不得盲目 retry。
所有错误必须脱敏且在 CollectionRun 可见。

验收方式：fixture/transport fault injection 覆盖 timeout、network、429、5xx、auth、forbidden、
not found、invalid payload 和未知异常；断言 run 状态、retry 决策和日志脱敏。

### FR-06 — Idempotency and RawItem Persistence

要求：stable external ID 优先形成确定性幂等身份；provider 无稳定 ID 时，fallback 必须在
文档中定义为来源范围内稳定、可测试且不依赖随机值。RawItem 必须关联当前 CollectionRun，
且只保存许可允许的最小 payload 或引用。

本 SPEC 的 duplicate protection 只保证重复 poll/retry 不产生不受控副作用；ContentItem
normalization 与 dedup 仍属于 SPEC-0006。

验收方式：重复 poll、retry、cursor replay 和有限 backfill 测试证明幂等，RawItem 来源/run/
account 关联正确，无孤立数据。

### FR-07 — Time Semantics

要求：严格区分来源 `published_at`、来源 `source_updated_at`、系统 `first_seen_at` 和本次
`fetched_at`。缺失值保持 null，不猜测；provider 时区必须明确并转换为 UTC。

验收方式：fixture 覆盖完整时间、缺失时间、非 UTC offset、更新记录和重复获取。

### FR-08 — Verification Evidence Gate

要求：真实 smoke test 只能在用户确认 provider、授权与接入政策后运行。结果必须记录日期、
endpoint 类别、授权状态、内容范围、请求数、RawItem/CollectionRun/cursor 结果和任何限制；
不得记录秘密或未授权正文。

验收方式：实施 PR 的 Delivery Report 提供真实证据；未授权或无法访问时必须明确 BLOCKED，
只能报告 contract/fixture 测试，不能声称真实接入通过。

## 9. 非功能需求

- 可靠性：重复 poll、retry 和重启保持幂等；cursor 只在 RawItem 安全持久化后推进。
- 性能：轮询间隔、batch 和 backfill 有界，遵守 provider rate limit。
- 安全：外部数据不可信；无登录/付费墙/captcha 绕过；秘密、cursor、query 和 payload 脱敏。
- 合规：access/license/retention 决定可获取与保存范围；blocked 必须 fail closed。
- 可替换性：删除该 adapter 不改变 runner 或 downstream contract。
- 可观测性：每次真实运行有 CollectionRun，失败不能表现为“没有新消息”。
- 实时性：只声明审核后的 Near Real-time、Standard 或 Periodic tier，不承诺 provider 未保证的 SLA。

## 10. 数据模型变化

无。

- 不创建 Alembic revision。
- 不新增或修改表、字段、enum、索引、外键或约束。
- 不修改现有 ORM 模型。
- 只使用既有 `Source`、`SourceAccount`、`CollectionCursor`、`CollectionRun` 和 `RawItem`。
- `access_level` / `license_policy` 使用第 7 节定义的现有 JSONB/retention 映射，不创建新字段。
- 实现验收必须证明 PostgreSQL schema 无 drift。

## 11. 接口与任务变化

- 新增：一个 provider-specific `CollectionAdapter` 实现及其 adapter key 注册。
- 复用：`CollectionTarget`、`CursorSnapshot`、`FetchRequest`、`RawItemEnvelope`、
  `FetchBatch`、`ClassifiedCollectionError`。
- 复用任务：`collection.dispatch_due_targets`、`collection.run_target`、
  `collection.recover_stale_runs`。
- API：无。
- CLI：无。
- Bot：无。
- Outbox / Notification：无行为变化。
- 新业务消息或 Internal Event Bus：无。

## 12. 配置变化

未来实现只允许增加该 provider 所需的无秘密配置模板和秘密引用名称。精确配置必须在 provider
确认后写入本文档，包括 endpoint、poll interval、timeout、batch limit、backfill limit 和
credential reference。

- 不提交真实 Token、Cookie、账号、订阅凭据或 `.env`。
- 不把秘密放入 Source/SourceAccount、fixture、日志或 Delivery Report。
- GDELT 的精确配置保持 `Pending verification before implementation`；不得预先添加其他
  provider 配置。

## 13. 错误处理

| Error Code | 条件 | Retry | 运维表现 |
|---|---|---|---|
| `COLLECTION_TIMEOUT` | provider 请求超时 | 是 | 统一 backoff，cursor 不推进 |
| `COLLECTION_NETWORK` | 临时网络失败 | 是 | 统一 backoff |
| `COLLECTION_RATE_LIMITED` | 429/明确限流 | 是 | 合法 Retry-After 或 capped backoff |
| `COLLECTION_UPSTREAM_5XX` | provider 5xx | 是 | 统一 backoff |
| `COLLECTION_UPSTREAM_RETRYABLE` | 408/425 或 contract 明确的临时错误 | 是 | 统一 backoff |
| `COLLECTION_AUTH` | 401 或缺失/失效授权 | 否 | failed；等待人工确认 |
| `COLLECTION_FORBIDDEN` | 403、robots/terms/access 阻塞 | 否 | failed/blocked；不得 fallback |
| `COLLECTION_NOT_FOUND` | endpoint/feed 404/410 | 否 | failed；人工核验 |
| `COLLECTION_CONTRACT_INVALID` | payload/cursor/envelope 不符合合同 | 否 | failed；开发错误可见 |
| `COLLECTION_CONFIG_INVALID` | provider 未确认、adapter 未注册或配置缺失 | 否 | 不运行或 failed |
| `COLLECTION_UNKNOWN` | 未分类异常 | 否 | failed；不得盲目 retry |

GDELT 映射必须为：429 → `COLLECTION_RATE_LIMITED`；408/425 →
`COLLECTION_UPSTREAM_RETRYABLE`；5xx → `COLLECTION_UPSTREAM_5XX`；transport timeout /
network 分别映射到对应稳定代码；401 → `COLLECTION_AUTH`；403 →
`COLLECTION_FORBIDDEN`；404/410 → `COLLECTION_NOT_FOUND`；无效 JSON、缺少必需 URL 或
envelope 不合法 → `COLLECTION_CONTRACT_INVALID`。其他未确认 4xx 不得猜测为可重试，未知
响应 fail closed 为 `COLLECTION_UNKNOWN`。这些 status/body 行为尚未真实请求验证。

其余数据库、lock、cancelled、stale-run 分类沿用 SPEC-0003。错误正文必须长度受限并脱敏。

## 14. Migration and Rollback

- 升级：无数据库迁移；未来实现只增加一个可移除 adapter、注册、fixture、配置模板和测试。
- 回滚：先禁用对应 Source/SourceAccount 并停止其任务，再移除 adapter 注册和配置；既有
  RawItem、CollectionRun 和 cursor 保留用于审计。
- 数据兼容：只写既有 Phase 1 表和字段，不改变 schema。
- 备份要求：无新 schema；真实或受限 payload 不得进入 Git、测试 fixture 或 Review ZIP。

## 15. Tasks

所有任务在本文档 Review PASS 且用户确认 provider 前保持未开始：

- [ ] T01 — 用户确认唯一试点 provider、Source/SourceAccount、合法接入与内容范围。
- [ ] T02 — 将第 7 节 Source Contract 的全部 Pending verification 项更新为已验证值并再次审核。
- [ ] T03 — 实现一个 provider-specific Polling Adapter/Connector 和唯一 adapter key。
- [ ] T04 — 映射统一 envelope、时间、cursor/watermark、access/license 和 retention。
- [ ] T05 — 实现 provider error mapping、rate limit、retry 和有限 backfill。
- [ ] T06 — 增加 fixture-based contract、cursor、幂等、访问政策和故障注入测试。
- [ ] T07 — 增加 PostgreSQL/Redis/Celery 集成与 no-schema-drift 验证。
- [ ] T08 — 授权确认后执行一次有界 real-source smoke test，并记录真实证据。
- [ ] T09 — 更新 Source Catalog、配置模板、Changelog 和 Delivery Report。

## 16. 测试要求

本轮文档准备不写测试代码。未来实现至少需要：

- Provider-neutral adapter contract test。
- Fixture 驱动且禁用外部网络的单元测试。
- 仅在授权确认后的有界 real-source smoke test。
- Cursor resume / watermark / checkpoint 原子性测试。
- Retry、Retry-After、backoff 和错误分类测试。
- Idempotency 与 duplicate protection 测试。
- `access_level` / `license_policy` mapping 和 retention 测试。
- RawItem persistence、CollectionRun 生命周期和 source/account 关联测试。
- PostgreSQL schema drift / Alembic check，证明无迁移。
- downstream 无 provider SDK、provider module 或 raw payload 专有字段依赖的边界测试。
- 禁止 paywall/login/captcha/robots/access-control bypass 的安全测试。
- failure visibility、日志脱敏和 blocked/fail-closed 测试。
- provider 未确认、未授权、未知 adapter 和第二来源不产生网络请求的测试。
- DOC 2.0 `ArticleList` JSON query 序列化测试，全程使用 mock/fixture，不访问网络。
- UTC closed-open window、受控 overlap、成功后 checkpoint 和失败不推进 watermark 测试。
- `MAXRECORDS <= 250` 测试；命中 250 条时必须缩小窗口或 fail visible，不得静默宣称完整。
- 不使用未文档化 cursor、page、offset 或 sequence 的 contract test。
- `LINK_ONLY` retention 测试：只允许 title/metadata/source URL/timestamps/ID/hash/reference，
  禁止正文、HTML、图片和完整 response。
- `sha256(source_id + exact returned URL)` 幂等与 overlap 重放测试；不做 URL canonicalization。
- publication date 缺失保持 null、`source_updated_at` 固定 null 的时间字段测试。
- GDELT attribution metadata 可追踪测试；不得把 GDELT Terms 当作第三方正文授权。
- 429/408/425/5xx/timeout/network/401/403/404/410/invalid JSON 错误映射测试；不得假设未公布
  的数字 rate limit、timeout 或 Retry-After。
- 获得单独授权前只用合法、脱敏、无秘密且不含受限全文的合成 fixture，并断言不访问 source
  page。

## 17. 验收标准

- [ ] 只接入一个由用户批准的合法 Polling Source。
- [ ] 首个 provider 已确认为 GDELT；官方文档合同已核验，所有 implementation blocker 已在
  获准的有界 smoke 中验证且不再处于 Pending。
- [ ] provider 可替换，且未成为 framework 或 downstream 的核心依赖。
- [ ] Source Adapter / Connector 将 provider 数据转换为统一内部 envelope。
- [ ] downstream 不导入 provider SDK，不读取 raw payload 专有字段。
- [ ] RawItem 可真实写入并正确关联 CollectionRun、Source 和 SourceAccount。
- [ ] CollectionRun 状态、计数、错误分类和重试正确。
- [ ] cursor / watermark 可安全恢复，只在持久化后推进。
- [ ] 重复 poll、retry 和有限 backfill 保持幂等。
- [ ] `access_level`、`license_policy` 和 retention 可追踪且被执行。
- [ ] 没有绕过付费墙、登录、captcha、robots、访问控制或 provider 限制。
- [ ] 未授权 Bloomberg / WSJ 全文不是成功条件，也未被采集。
- [ ] 未新增 schema、ORM 变化或 Alembic revision。
- [ ] 未实现 ContentItem normalization、Deduplication、Notification 或 Outbox 行为。
- [ ] 未实现 Streaming、Webhook、X、Telegram、Event、AI、Market Validation 或 Research Recommendation。
- [ ] 所有验证命令和 real-source smoke test 结果真实记录；未运行不得写成 PASS。
- [ ] `git diff --check`、Foundation validation、静态/测试/schema/runtime 验证按实现风险通过。
- [ ] GDELT 使用 DOC 2.0 `ArticleList` JSON、`LINK_ONLY` 和最小 retention，不保存新闻正文。
- [ ] 时间窗口为 UTC closed-open，可恢复且命中 250 条封顶时缩窗或 fail visible。
- [ ] exact source URL fallback idempotency、overlap replay 和 nullable timestamps 通过 contract
  tests。
- [ ] 未假设 cursor/offset/sequence、数字 rate limit、timeout、Retry-After 或 DOC API SLA。
- [ ] 本轮文档核验没有运行真实 collection、保存真实 GDELT 数据或访问新闻来源页面。

## 18. Verification Evidence

| Task/Requirement | 命令或证据 | 结果 | 日期 |
|---|---|---|---|
| 文档准备：diff | `git diff --check` | PASS | 2026-07-29 |
| 文档准备：Foundation | `python3 scripts/validate-foundation.py` | PASS（Required files: 20；Markdown links: valid；Freeze markers: valid） | 2026-07-29 |
| Provider confirmation | 用户明确选择 GDELT 作为首个 Polling Source Pilot candidate | PASS（仅 provider selection；不代表授权或实现批准） | 2026-07-29 |
| Official GDELT contract verification | DOC 2.0 API、GDELT 2.0、API Rate Limiting、Terms of Use、Event Codebook；详见 7.1 | PARTIAL PASS：endpoint family、时间窗口、记录上限、rate-limit 存在、terms/attribution、LINK_ONLY 约束已核验 | 2026-07-29 |
| Bounded smoke verification | 2 次 DOC 2.0 尝试：1 次 HTTP 429、1 次 SSL connection timeout；`maxrecords=1`、`timespan=15m` | PARTIAL PASS：可达性和 429 已观察；成功 JSON/schema 仍 Blocked | 2026-07-29 |
| No prohibited source activity | 未访问 source page、未运行 collection、未保存完整 response、真实 GDELT 数据或新闻正文 | PASS | 2026-07-29 |
| Implementation | 第 16–17 节要求 | Not started | |

## 19. Commit Evidence

| 类型 | Commit |
|---|---|
| Specification activation | 由本 Draft PR 提交记录证明 |
| Implementation | 无；implementation not started |
| Review fixes | 无 |

## 20. 实现结果

未开始。本轮只执行两次获准的 DOC 2.0 bounded smoke attempt 并记录结构性结果；没有写代码、
访问新闻来源页面、运行 collection、保存完整 response 或保存真实 GDELT 数据。

## 21. 与 SPEC 的偏差

无。

## 22. 已知问题与风险

- Blocker：429 与 SSL connection timeout 使本轮未获得成功响应。进入 implementation 前仍须
  核验成功 JSON 的顶层/article schema、nullability/time semantics、实际无 API key 行为、
  `maxrecords`/`timespan` 执行效果、长期可用性和 retention duration。
- Pending：numeric rate limit/window、其他响应中的 `Retry-After`、provider timeout 建议、
  rolling window 当前边界和除 429 外的真实错误格式；不得从本次未观察到推断其不存在。
- 已核验约束：DOC 2.0 `ArticleList` JSON endpoint family、时间窗口、250 条上限、rate limiting
  存在、GDELT citation/link 条款、`LINK_ONLY` 与最小 retention。它们不构成第三方新闻全文授权。
- Improvement：无。
- Future Scope：第二 provider、Streaming、Webhook、通用 Historical Backfill、X、
  Normalization/Dedup/Outbox、Telegram、Event、AI、Market Validation、Research
  Recommendation 均属于其他 SPEC。

## 23. Review History

| 轮次 | 结果 | 主要问题 | 处理 |
|---|---|---|---|
| 0 | Pending | 初始文档准备；provider 尚待用户确认 | 未开始实现 |
| 1 | REQUEST CHANGES | 首个 provider 未确认，Source Contract 存在实现必需未决项 | 用户选择 GDELT；未核验事项明确标为 implementation blocker |
| 2 | Preimplementation verification | 需要以官方证据收敛 GDELT contract，同时禁止真实请求 | 完成官方文档核验；runtime 项保持 Blocked，implementation 未开始 |
| 3 | Bounded smoke verification | 最多三次极小请求核验 runtime blocker | 实际尝试两次：429、SSL timeout；部分证据已记录，其余保持 Blocked |

## 24. 架构与治理检查

- Active Foundation 版本：v2.1-FROZEN。
- 是否依赖 Proposed Decision：仅引用 provider-neutral 方向作为接口约束，不启用 D-020–D-024
  的新基础设施或未来阶段能力。
- 是否需要先完成 Freeze Review：本 SPEC 的单一合法 Polling Source 与 Phase 1 一致，不改变
  Foundation；Event/AI/Market Validation/Research Recommendation 等禁止项仍需未来流程。
- 是否改变阶段边界、schema 或既有实体：否。
- 外部 Provider / Connector 及授权状态：GDELT Polling candidate 已选择；DOC 2.0 endpoint
  family 已由官方文档核验，runtime/auth behavior 仍 Blocked pending separate authorization。
- 数据许可、保存与再分发限制：GDELT 数据集 citation/link 条款已核验；不推导第三方新闻内容
  权利，固定 `LINK_ONLY`、最小化、fail closed。
- 采集模式：只允许 Polling。
- cursor、sequence、ack、retry 与 recovery：使用 SPEC-0003 contract；sequence 若无则 null，
  有限 backfill 由 provider 合同限定。
- 是否保持未知或未授权能力 fail closed：是。
